{{/*
Copyright 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
SPDX-License-Identifier: Apache-2.0
*/}}
apiVersion: gpu-fractioning.kai.scheduler/v1alpha1
kind: GpuFractioningConfig
metadata:
  name: default
  labels:
    {{- include "gpu-fractioning.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": post-install,post-upgrade
    "helm.sh/hook-weight": "0"
spec:
  nodeSelector:
    nvidia.com/gpu.present: "true"
  # Present (even if empty) so the API server applies field-level defaults such
  # as fractioningAgent.retroactiveEnforcement=true.
  {{- if and .Values.fractioningAgent .Values.fractioningAgent.logLevel }}
  fractioningAgent:
    logLevel: {{ .Values.fractioningAgent.logLevel | quote }}
  {{- else }}
  fractioningAgent: {}
  {{- end }}
  metricsAgent:
    enabled: {{ .Values.metricsAgent.enabled }}
    {{- if hasKey .Values.metricsAgent "runtimeClassName" }}
    runtimeClassName: {{ .Values.metricsAgent.runtimeClassName | quote }}
    {{- end }}
    {{- if .Values.metricsAgent.logLevel }}
    logLevel: {{ .Values.metricsAgent.logLevel | quote }}
    {{- end }}
    {{- with .Values.metricsAgent.metricNames }}
    metricNames:
      {{- toYaml . | nindent 6 }}
    {{- end }}
    {{- if .Values.metricsAgent.volumes }}
    volumes:
      {{- toYaml .Values.metricsAgent.volumes | nindent 6 }}
    {{- end }}
    {{- if .Values.metricsAgent.volumeMounts }}
    volumeMounts:
      {{- toYaml .Values.metricsAgent.volumeMounts | nindent 6 }}
    {{- end }}
  {{- if hasKey .Values.mpsd "runtimeClassName" }}
  mpsDaemon:
    runtimeClassName: {{ .Values.mpsd.runtimeClassName | quote }}
  {{- end }}
