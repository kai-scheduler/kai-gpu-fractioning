{{- if .Values.metrics.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "gpu-sharing.fullname" . }}-metrics
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "gpu-sharing.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "gpu-sharing.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.metrics.port }}
      targetPort: metrics
      protocol: TCP
{{- end }}
